一、运行环境：
（1） 在mysql连接的情况下
（2） 在Tomcat服务器中运行
（3） 在java环境中运行
（4） 数据库+服务器+静态页面
（5） 数据通过表单向服务器发送请求===》服务器向数据库中获取资源===》响应回来===》显示在用户界面上
（6） 服务器的种类（node）
（7） 项目文件java文件+servlet中的jsp文件+static中的html js css img文件+mysql文件
（8） 项目流程将上诉文件写好之后 将所有的web相关文件托管到Tomcat服务器中
（9） 托管资源方式：启动服务器（run/stop servers）===》托管资源（project to server）===》访问数据库===》获取资源
（10）通过主机名+路径名访问资源
二、执行流程：
（1）lib中的6个jar包是解决json问题的
（2）